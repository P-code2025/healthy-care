[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["cd server && npm ci --only=production"]

[phases.build]
# Explicitly skip the build phase - we don't need to build anything
cmds = []

[start]
cmd = "node server/api-proxy.js"
